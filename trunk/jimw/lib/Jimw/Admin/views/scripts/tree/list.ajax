<?php
	//echo '[';
	$first = true;
 	foreach ($this->list_tree as $tree) {
 		if ($first) $first = false;
 		else echo ', ';
 		echo '{"alias" : "' . $tree['tree_alias'] . '", "title" : "' . $tree['tree_menutitle'] . ' (' . $tree['tree_id'] . ')' . '", "id" : ' . $tree['tree_id'] . '}';
 	}
 	//echo ']';